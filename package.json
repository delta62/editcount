{
  "name": "@noedel/editcount",
  "version": "1.0.3",
  "scripts": {
    "start": "jest --watch",
    "test": "jest",
    "build": "tsc",
    "prepublishOnly": "yarn run build"
  },
  "license": "MIT",
  "typings": "lib/index.d.ts",
  "repository": "delta62/editcount",
  "devDependencies": {
    "@types/node": "^11.9.5",
    "jest": "^24.1.0",
    "ts-jest": "^24.0.0",
    "typescript": "^3.3.3"
  },
  "files": [
    "lib/**/*.js",
    "lib/**/*.d.ts"
  ],
  "dependencies": {
    "rxjs": "^6.4.0"
  },
  "jest": {
    "moduleFileExtensions": [
      "ts",
      "js"
    ],
    "transform": {
      "\\.ts$": "ts-jest"
    }
  }
}
